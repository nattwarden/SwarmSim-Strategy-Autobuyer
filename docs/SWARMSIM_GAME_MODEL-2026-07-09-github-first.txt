# SwarmSim Game Model — GitHub-first

Version: 2026-07-09-github-first
Status: Canonical strategy model for SwarmSim Strategy Autobuyer baseline 0.8.0.

## Source-of-truth policy

GitHub repository state is source of truth.

From 0.8.0 onward:

- Canonical and only executable script source:
  - `src/SwarmSim-Strategy-Autobuyer.user.js`
- Documentation lives in `.md` files.
- Release history is tracked through Git commits/tags.
- `releases/` folders are documentation-only.
- Do not create `.txt` script mirrors.
- Do not create duplicated `.user.js` release copies.

## Current baseline

- Script: SwarmSim Strategy Autobuyer
- Baseline: 0.8.0
- Focus: conservative Smart Mode planning
- New in baseline: Unlock Planner, Clone Buffer Planner, Ability Prep Planner (advisor-only)

## Safety and behavior constraints

Keep conservative defaults and guardrails:

- no default ability auto-cast
- no default Clone Larvae auto-cast
- no default House of Mirrors auto-cast
- no default auto-ascend
- no default Nightbug/Bat auto-buy
- no aggressive Smart Mode buyMax for meat-chain planning
- preserve reserve/payback protections
- preserve active-action floor and fallback safeguards

## Planning layers

Priority intent (high to lower):

1. Engine/Hatchery/Expansion saves and buys
2. Nexus/energy protection
3. Clone post-cast buffer recovery
4. Unlock Planner (conservative unlock-value gate)
5. Target-aware upgrade/twin planner
6. Meat goal planner and fallback
7. Territory ROI
8. Clone prep side-task
9. Ability prep advisor-only

## Clone buffer arithmetic requirement

For clone buffer debt/protection/spendable larvae, use Decimal arithmetic end-to-end.

Shape:

```js
const rawDebt = target.minus(cocoons);
const debt = rawDebt.greaterThan(0) ? rawDebt : newDecimal(0);

const desiredProtected = debt.times(protectRatio);
const larvaeProtected = decimalMin(larvae, desiredProtected);

const rawSpendable = larvae.minus(larvaeProtected);
const spendableLarvae = rawSpendable.greaterThan(0) ? rawSpendable : newDecimal(0);
```

## Release/install guidance

When users need an installable version, use:

- `src/SwarmSim-Strategy-Autobuyer.user.js` at the desired Git tag/commit
- GitHub release/tag archive for that commit

Do not create duplicated script artifacts in `releases/`.
